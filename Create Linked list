#inlude<stdio.h>
#include<stdlib.h>

struct node{
int i;
struct node *link;
};

int main(){
struct node *head=malloc(sizeof(struct node));

head->data=45; 
head->link=NULL;

struct noe *head2=malloc(sizeof?(struct node)):
head2->data=98;
head2->link=NULL;

head->link=head2;
}

